const config = {
  singleQuote: false,
  tabWidth: 2,
  printWidth: 80,
  semi: true,
  arrowParens: "always",
  bracketSpacing: true,
  useTabs: false,
  singleAttributePerLine: true,
  plugins: ["prettier-plugin-tailwindcss"],
};

export default config;
